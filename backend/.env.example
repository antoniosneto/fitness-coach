# PostgreSQL – ajuste para seu ambiente local ou CI
DATABASE_URL="postgresql://user:password@localhost:5432/fitness_coach?schema=public"

# JWT (será usado nos próximos PRs)
# JWT_SECRET=
# JWT_EXPIRES_IN=7d
